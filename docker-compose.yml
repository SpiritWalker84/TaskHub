# TaskHub — порты смещены (на стандартных крутится React)
# Web: 8080, MySQL: 3307, Redis: 6380

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: taskhub-app
    restart: unless-stopped
    working_dir: /var/www
    volumes:
      - .:/var/www
      - ./storage/logs:/var/www/storage/logs
    environment:
      - APP_ENV=${APP_ENV:-production}
      - APP_DEBUG=${APP_DEBUG:-false}
      - APP_KEY=${APP_KEY}
    env_file:
      - .env
    depends_on:
      - mysql
    networks:
      - taskhub

  nginx:
    image: nginx:alpine
    container_name: taskhub-nginx
    restart: unless-stopped
    ports:
      - "${APP_PORT:-8080}:80"
    volumes:
      - .:/var/www
      - ./.docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - app
    networks:
      - taskhub

  mysql:
    image: mysql:8.0
    container_name: taskhub-mysql
    restart: unless-stopped
    ports:
      - "${DB_PORT:-3307}:3306"
    environment:
      MYSQL_DATABASE: ${DB_DATABASE}
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
      MYSQL_USER: ${DB_USERNAME}
      MYSQL_PASSWORD: ${DB_PASSWORD}
    volumes:
      - taskhub_mysql_data:/var/lib/mysql
    networks:
      - taskhub

  # Опционально: очередь и кэш (порт смещён)
  redis:
    image: redis:alpine
    container_name: taskhub-redis
    restart: unless-stopped
    ports:
      - "${REDIS_PORT:-6380}:6379"
    networks:
      - taskhub

volumes:
  taskhub_mysql_data:

networks:
  taskhub:
    driver: bridge
